package ch06;

public class VariableTest1 {

	public static void main(String[] args) {
		int age, count;
		age = 10;
		
		int level = 10000;
		
		System.out.println(age);
		System.out.println(level);
		
		/* - age 변수를 int 타입으로 미리 선언해두고,
		     밑에서 지역변수로 10이라는 값을 대입하여 결과를 출력헀다.
		   
		   - int 타입으로 level이라는 변수를 선언하여 값을 대입했으므로
		     level의 값은 10000이 된다.
		 */
		
	}

}
